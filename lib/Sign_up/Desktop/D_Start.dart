import 'package:flutter/material.dart';

class D_Start extends StatelessWidget {
  const D_Start({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}