import 'package:flutter/material.dart';

class LandingEnroll extends StatelessWidget {
  const LandingEnroll({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}